services:
  rails:
    build:
      context: .
      dockerfile: Dockerfile
    image:
      contracttocure-production
    container_name: contracttocure
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "3103:4567"
    environment:
      - PORT=4567
    env_file:
      - .env
    volumes:
      - contracttocure_logs:/app/log
      - contracttocure_tmp:/app/tmp
    restart: unless-stopped
    command: ["bundle", "exec", "puma", "-C", "config/puma.rb"]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4567/game_state.json"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  contracttocure_logs:
  contracttocure_tmp:
